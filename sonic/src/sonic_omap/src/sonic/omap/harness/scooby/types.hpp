#pragma once

#include <cstddef>
#include <cstdint>

namespace sn::omap::harness::scooby {

constexpr std::size_t kScoobyPosmapBucketSize = 64;
using key_type = std::uint32_t;

} // namespace sn::omap::harness::scooby
